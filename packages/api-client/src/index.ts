/**
 * The API contract, shared by the web app and the API's own tests.
 *
 * Transport lives with the caller; see `endpoints.ts` for why.
 */
export * from './schemas.js';
export * from './endpoints.js';
