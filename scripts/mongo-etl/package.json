{
  "name": "@nexmarket/mongo-etl",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "type-check": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "etl": "tsx src/main.ts"
  },
  "dependencies": {
    "@nexmarket/shared": "workspace:*",
    "mongodb": "^7.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@nexmarket/config": "workspace:*",
    "tsx": "^4.23.12",
    "vitest": "^4.1.11"
  }
}
